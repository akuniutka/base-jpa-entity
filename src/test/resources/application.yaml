spring:
  main:
    banner-mode: off
  datasource:
    url: jdbc:h2:mem:test
    username: test
    password: test
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
